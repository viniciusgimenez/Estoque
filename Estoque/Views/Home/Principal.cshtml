
@{
    ViewBag.Title = "Principal";
}

<h2>Principal</h2>
@if (Session["Nome"] != null)
{
    <p>
        <h3>Bem vindo ao sistema</h3> 
        <h1>
            @Session["Nome"].ToString()
        </h1>
    
    @Html.ActionLink("Sair", "Index", "Home")</p>
}
