@model Estoque.Models.Linha

@{
    ViewBag.Title = "Detalhes";
}

<h2>Visualizar</h2>
<div>
    <h4>Linha</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.DESCRICAO)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DESCRICAO)
        </dd>

    </dl>
</div>
<p>
    <button class="btn warning">
        @Html.ActionLink("Edit", "Edit", new { id = Model.IDLINHA }, new { @class = "warning" })
    </button> |
    <button class="btn default">
        @Html.ActionLink("Voltar", "Index", new { @class = "default" })
    </button>
</p>
