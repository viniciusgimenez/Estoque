@model Estoque.Models.TipoMovimento

@{
    ViewBag.Title = "Apagar";
}

<h2>Apagar</h2>

<h3>Tem certeza que deseja apagar esse Tipo de Movimento de Estoque?</h3>
<div>
    <h4> Tipo de Movimento de Estoque</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.DESCRICAO)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DESCRICAO)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ENTRADA)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ENTRADA)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <button type="submit" value="Delete" class="btn danger">Apagar</button> |
            <button class="btn default">
                @Html.ActionLink("Voltar", "Index")
            </button>
        </div>
    }
</div>
