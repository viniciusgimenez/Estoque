@model IEnumerable<Estoque.Models.TipoUsuario>

@{
    ViewBag.Title = "Tipo de Usuário";
}

<h2>Tipo de Usuário</h2>

<p>
    <button class="btn default">
        @Html.ActionLink("Novo", "Create", new { @class = "default" })
    </button>
</p>
@using (Html.BeginForm())
{
    <p>
        Filtro: @Html.TextBox("filtro")
        <button type="submit" value="filtro" class="btn info">Pesquisar</button>
    </p>
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DESCRICAO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ADMINISTRATIVO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ALTERA)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DESCRICAO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ADMINISTRATIVO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ALTERA)
            </td>
            <td>
                <button class="btn warning">
                    @Html.ActionLink("Editar", "Edit", new { id = item.IDTIPOUSUARIO }, new { @class = "warning" })
                </button> |
                <button class="btn info">
                    @Html.ActionLink("Visualizar", "Details", new { id = item.IDTIPOUSUARIO }, new { @class = "info" })
                </button> |
                <button class="btn danger">
                    @Html.ActionLink("Apagar", "Delete", new { id = item.IDTIPOUSUARIO }, new { @class = "danger" })
                </button>
            </td>
        </tr>
    }

</table>
